#include<bits/stdc++.h>
using namespace std;
const int MAXN=250010;
typedef long long LL;
LL n,ans;
LL d[MAXN],w[MAXN];

int main()
{
	scanf("%lld",&n);
	for(int i=1;i<=n;i++)
		scanf("%lld%lld",&d[i],&w[i]);
	ans=1;
	for(int i=2;i<=n;i++)
		if(w[i]>w[i-1]) ans++;
	printf("%lld",ans);
	return 0;
}
